echo "Deploying to $2"
echo "Make sure to restart server in production mode at $1"

RAILS_ENV=production bundle exec rake assets:clobber assets:precompile

curl http://$1/ -o ./public/index.html

rsync -rv public $2/

rm -rf public/index.html public/assets

